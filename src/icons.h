#pragma once

#include <lvgl.h>

/* Auto-generated I1 bitmaps for the mono display: bit 1 = paper
 * (white), bit 0 = ink (the glyph). They blend bit-exact through the
 * custom I1 decoder (i1_decoder.c) - no recolor styling needed.
 */
extern const lv_image_dsc_t icon_play;
extern const lv_image_dsc_t icon_pause;
extern const lv_image_dsc_t icon_shuffle_off;
extern const lv_image_dsc_t icon_shuffle_on;
extern const lv_image_dsc_t icon_loop_off;
extern const lv_image_dsc_t icon_loop_on;
extern const lv_image_dsc_t icon_note;
